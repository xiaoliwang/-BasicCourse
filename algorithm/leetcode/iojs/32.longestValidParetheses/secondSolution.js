/**
 * @param {string} s
 * @return {number}
 */
var longestValidParentheses = function(s) {
    var len = s.length;
	if (len <= 1)
		return 0;
	var max = 0;
	var longest = new array(len);
	for (var i = 1; i < len; ++i) {
		if (s[i] === ')' && )
	}
};